import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    webInputContainer: {
        borderColor: '#e5e5e5', 
        borderWidth: 2,
        marginTop: 30, 
        padding: 10,
        width: '100%',
        height: '100%',
    },

    inputValue: {
        fontSize: 16,
        fontWeight: 'bold',
        marginTop: 5,
        height: '50%'
    },
})